project.name=trader
project.home=/Users/towne/Documents/Trader-Server
deploy.dir=/data/webapps

debug=off
optimize=on
deprecation=on

src.dir=${project.home}/src/main/java
config.dir=${project.home}/src/main/resources
web.dir=${project.home}/src/main/webapp
lib.dir=${project.home}/lib

target.dir=${project.home}/target
compile.dir=${target.dir}/classes
build.dir=${target.dir}/${project.name}


